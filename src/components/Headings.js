import styled from 'styled-components'
import colors from '../style/colors'

const headingColor = colors.primary4

export const H2 = styled.h2`
  font-size: 2.8rem;
  line-height: 1.2;
  color: ${headingColor};
  text-align: center;
`
